name: 04 - Using Actions
on: 
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v7
      - name: Printing Folders
        run: |
          echo "Printing the folders in the current directory"
          ls -al
