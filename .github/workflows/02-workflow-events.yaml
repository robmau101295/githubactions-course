name: 02 - Workflow Events
on:
#  push:
#  pull_request:
#    types: [opened, reopened]
#  schedule:
#    - cron: "15 4,5 * * *"  
  workflow_dispatch:

jobs: 
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show the trigger
        run: |
          echo "The event that triggered this workflow is:"
          echo ${{ github.event_name }}
    
